<UserControl  x:Class="WLS3200Gen2.UserControls.CassetteUnitUC"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        x:Name="CassetteUnit"
        mc:Ignorable="d" Height="14" Width="180"   >

    <Grid DataContext="{Binding ElementName=CassetteUnit}" >
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="0.1*"/>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding WaferInfo}" Grid.Column="0" FontSize="8" Width="11"/>
        <Button Margin="2,0,2,0"   Grid.Column="1"  Command="{Binding Top_Command}"  Height="14"     />
        <Button Margin="2,0,2,0"  Grid.Column="2"  Command="{Binding Back_Command}"   Height="14"    />
        <Button Margin="2,0,2,0"   Grid.Column="3"    Command="{Binding Micro_Command}"  Height="14"  />

    </Grid>
</UserControl>
